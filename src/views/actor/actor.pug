extends ../layout

block content
  .container.mt-5
    .row
      .col-md-8.offset-md-2
        .text-center
          h1.display-4.mb-3= `${actor.first_name} ${actor.last_name}`
          hr.my-4
          h1.display-4.mb-3 Films
          .row.mt-4
            each film in actor.films
              include ../components/film_card